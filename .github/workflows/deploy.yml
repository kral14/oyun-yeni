# Heroku deployment workflow - DISABLED
# GitHub Pages istifadə edirik, Heroku istifadə edilmir
name: Deploy to Production (Disabled)

on:
  workflow_dispatch:
    # Manual trigger only - disabled by default

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: false  # Disabled
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Deploy disabled
      run: |
        echo "Heroku deployment is disabled. Use GitHub Pages instead."

